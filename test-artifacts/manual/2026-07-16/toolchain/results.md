# Toolchain manual evidence — incomplete

This session was interrupted while computer-control window selection was being debugged. It is **not counted as a manual section pass** in `EXTENSION_HEALTH.md`.

## Captured evidence

- `01-real-ready.jpeg`: the real local toolchain rendered Android CLI `1.0.15498356` and Android Debug Bridge `1.0.41` as Ready.

## Missing before this section can pass

- repeat the real-ready procedure intentionally;
- capture CLI missing;
- capture ADB missing;
- capture CLI installed with `android info` failing;
- record expected/actual results and the extension revision for each state.
